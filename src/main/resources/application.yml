spring:
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher
  profiles:
    active: local
  datasource:
    url: jdbc:mysql://localhost:3306/firefox?useSSL=false&allowPublicKeyRetrieval=true&useUnicode=true&serverTimezone=Asia/Seoul
    username: root
    password: 2512
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        format_sql: true
        default_batch_fetch_size: 100
    open-in-view: false

logging:
  level:
    com.firewolf.cont : info
    org.hibernate.SQL : debug
    org.springframework.orm.jpa.JpaTransactionManager: debug
#    org.hibernate.type : trace

#kakao
kakao:
  client_id: b182ecd9e6b3167982c8a6214f6fe9a8
  redirect_uri: http://localhost:8080/mainPage